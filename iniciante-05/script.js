const a = Number(prompt('Digite o primeiro valor: '))
const b = Number(prompt('Digite o segundo valor: '))

const resultado = Math.pow(a - b, 2)

alert(`O resultado do quadrado da diferença entre A e B é: ${resultado}`)
